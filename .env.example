# --------------------------------------------------
# MetaSAAS Environment Variables
# Copy this file to .env and fill in values.
# --------------------------------------------------

# PostgreSQL
POSTGRES_USER=metasaas
POSTGRES_PASSWORD=metasaas
POSTGRES_DB=metasaas
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# Derived database URL used by Drizzle ORM
DATABASE_URL=postgresql://metasaas:metasaas@localhost:5433/metasaas

# API Server
API_PORT=4000
API_HOST=0.0.0.0

# Web Frontend
NEXT_PUBLIC_API_URL=http://localhost:4000

# --------------------------------------------------
# Supabase Auth (optional — app works without it, uses dev mode)
# Uncomment and fill in your Supabase project values to enable authentication.
# --------------------------------------------------
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_SERVICE_KEY=eyJ...your-service-role-key
# NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key

# --------------------------------------------------
# AI Gateway (optional — app works without it, AI capabilities use fallbacks)
#
# AI_PROVIDER selects the provider explicitly: gemini | openai | anthropic
# If omitted, auto-detects by checking keys: Gemini → OpenAI → Anthropic → None
# --------------------------------------------------
# AI_PROVIDER=gemini
# GOOGLE_AI_API_KEY=your-google-ai-api-key
# GOOGLE_AI_MODEL=gemini-2.0-flash
# OPENAI_API_KEY=sk-your-openai-key
# OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
