# --------------------------------------------------
# MetaSAAS Environment Variables
# Copy this file to .env and fill in your values.
# --------------------------------------------------

# PostgreSQL
POSTGRES_USER=metasaas
POSTGRES_PASSWORD=metasaas
POSTGRES_DB=metasaas
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# Derived database URL used by Drizzle ORM
DATABASE_URL=postgresql://metasaas:metasaas@localhost:5433/metasaas

# API Server
API_PORT=4001
API_HOST=0.0.0.0

# Web Frontend
NEXT_PUBLIC_API_URL=http://localhost:4001

# CORS — comma-separated origins allowed to call the API
# CORS_ORIGIN=http://localhost:3001

# --------------------------------------------------
# Supabase Auth (optional — app works without it, uses dev mode)
# --------------------------------------------------
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --------------------------------------------------
# AI Gateway (optional — app works without it)
#
# AI_PROVIDER: gemini | openai | anthropic
# If omitted, auto-detects by checking which key is set.
# --------------------------------------------------
# AI_PROVIDER=gemini
# GOOGLE_AI_API_KEY=your-google-ai-key
# OPENAI_API_KEY=sk-your-openai-key
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# --------------------------------------------------
# Email (optional — app works without it, logs emails to console)
#
# Provider: Resend (https://resend.com — free for 3K emails/month)
# Without RESEND_API_KEY, emails are logged to console (dev mode).
# --------------------------------------------------
# RESEND_API_KEY=re_your-resend-api-key
# EMAIL_FROM=YourApp <noreply@yourdomain.com>

# --------------------------------------------------
# File Storage (optional — app works without it, saves to ./uploads/)
#
# Provider: S3-compatible (AWS S3, Cloudflare R2, MinIO)
# Without STORAGE_ENDPOINT, files are saved locally to ./uploads/
# --------------------------------------------------
# STORAGE_ENDPOINT=https://your-s3-endpoint.com
# STORAGE_BUCKET=your-bucket-name
# STORAGE_ACCESS_KEY=your-access-key
# STORAGE_SECRET_KEY=your-secret-key
# STORAGE_REGION=auto
# STORAGE_PUBLIC_URL=https://cdn.yourdomain.com

# --------------------------------------------------
# Observability (optional — app works without it, logs to console)
#
# Provider: Sentry (https://sentry.io)
# Without SENTRY_DSN, errors and logs go to console (dev mode).
# --------------------------------------------------
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# SENTRY_ENVIRONMENT=production

# --------------------------------------------------
# License Key (optional — free tier without it)
#
# Get a license key at https://metasaas.dev/pricing
# Unlocks: Billing (Stripe), SSO/SAML, White-label, Advanced RBAC.
# --------------------------------------------------
# METASAAS_LICENSE_KEY=your-license-key

# --------------------------------------------------
# Production (used by docker-compose.prod.yml)
# --------------------------------------------------
# WEB_PORT=3001
# API_PORT=4001
