/**
 * Test Setup — @metasaas/web
 *
 * Runs before every test file. Configures:
 * - jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.)
 * - Global fetch mock reset
 */

import "@testing-library/jest-dom/vitest";
