{
  "name": "@metasaas/api",
  "version": "0.0.1",
  "private": true,
  "description": "Fastify backend — wires platform + domain, serves REST API",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "db:migrate": "tsx src/migrate.ts",
    "db:seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@metasaas/contracts": "workspace:*",
    "@metasaas/domain": "workspace:*",
    "@metasaas/platform": "workspace:*",
    "dotenv": "^16",
    "fastify": "^5"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5.7"
  }
}
